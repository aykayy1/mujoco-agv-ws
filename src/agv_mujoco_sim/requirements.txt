# Install the correct CPU/CUDA PyTorch build for each machine first.
# Stable-Baselines3 will reuse that installation.
numpy==1.26.4
gymnasium==1.3.0
stable-baselines3[extra]==2.9.0

# MuJoCo Python API used by mujoco_cmd_vel_bridge.py.
mujoco>=3.2,<4

# Used by validation/tests and available as python3-yaml on ROS 2 Humble.
pyyaml>=6,<7
